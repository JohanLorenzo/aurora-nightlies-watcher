{
  "credentials": {
    "client_id": "YOUR_CLIENT_ID",
    "access_token": "YOUR_ACCESS_TOKEN"
  },
  "task": {
    "owner": "YOUR_EMAIL",
    "treeherder": {
      "reason": "A reason why this job was created"
    },
    "provisioner_id": "provisioner-on-which-tasks-will-run",
    "worker_type": "workers-on-which-tasks-will-run",
    "scopes": ["some:scopes"],
  },
  "pulse": {
    "user": "YOUR_USER",
    "password": "YOUR_PASSWORD",
  }
}
